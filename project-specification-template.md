[fabricates project] 개발 명세서

1. 프로젝트 개요 (Project Overview)
목표: 3D 환경에 FAB LAYOUT과 FAB을 구성하는 설비의 배치 및 설비 조작 시뮬레이션을 할 수 있는 웹 기반의 툴을 개발한다.
타겟 사용자: 모든 현장 엔지니어를 대상으로 한다.
주요 기능 요약: FAB MODE와 EQP MODE를 지원한다. FAB MODE에서는 FAB LAYOUT을 3D 환경에서 확인하고, 설비의 배치를 시뮬레이션 할 수 있어야 한다. EQP MODE에서는 설비의 조작을 시뮬레이션 할 수 있어야 한다.

2. 기술 스택 (Tech Stack)
원하는 기술 스택이 있다면 구체적으로 명시해주세요. 없다면 "추천 요망"이라고 적어주세요.

Frontend: R3F, React, VITE(?)
Backend: 추천 요망
Database: json
Styling: 추천 요망
기타: internet browser에서 동작해야 한다.

3. 기능 요구사항 (Functional Requirements)

3.1 FAB MODE
설명: FAB의 LAYOUT을 3D 환경에서 확인하고, 설비의 배치를 시뮬레이션 할 수 있어야 한다.
입력: FAB에서 찾고자 하는 설비이름을 검색한다.
동작: 해당 설비를 FAB에서 찾아 카메라를 해당 위치로 이동한다.
출력/결과: 설비에 마우스를 올리면 해당 설비의 세부적인 정보를 보여준다
예외 처리: 설비가 없으면 "설비가 없습니다"라는 메시지를 보여준다. 검색을 하지 않는 경우에는 키보드 조작을 통해 FAB을 자유롭게 둘러볼 수 있어야 한다.

3.2 EQP MODE
설명: 사용자가 설비의 조작을 시뮬레이션 할 수 있어야 한다. 화면의 가운데에는 3D 설비 객체들을, 화면의 오른쪽에는 설비의 조작을 위한 인터페이스를 배치한다.
입력: 설비 조작을 위한 버튼을 클릭한다
동작: 버튼을 클릭한대로 설비의 동작을 시뮬레이션 한다(예:CHEMICAL DISPENSE NOZZLE의 경우 노즐이 위아래로 움직인다). 
출력/결과: 설비의 동작을 사용자에게 3D 환경에서 보여준다.
예외 처리: 해당 동작은 불가능하다는 메시지를 보여준다.

4. UI/UX 디자인 (Design Guidelines)
전반적인 스타일: minimal, nordic design
참고 사이트/앱: 없음.
레이아웃: 추천요망
색상 테마: 배경은 검은색으로 구성하고, 텍스트는 흰색으로 구성한다. fab mode에서 설비의 객체는 흰색으로 구성하고, eqp mode에서 설비를 구성하는 객체들은 회색으로 구성한다.

5. 데이터 구조 (Data Structure)
fab mode에서는 json 파일로 fab mode에 배치할 설비들의 정보(설비크기, 공정명, 설비명, 설비의 위치 등)를 관리한다. 
eqp mode에서 설비의 객체들을 구성하는 객체들은 외부에서 blender로 구현되어 glb나 gltf로 저장되고,
해당 프로젝트에서는 gltfjsx를 통해 불러와 사용한다.

6. 제약 사항 및 기타 (Constraints & Others)
브라우저 지원: 모든 browser에서 사용가능할 것
성능 요구사항: GPU가 없는 컴퓨터에서도 버벅거림 없이 구동할 것
라이선스 유무: 없음
기타 요청사항: 없음
